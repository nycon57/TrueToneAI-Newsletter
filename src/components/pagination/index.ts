/**
 * Pagination Components
 *
 * Two pagination options for the TrueTone AI Newsletter platform:
 *
 * - **InfiniteScroll**: Mobile-first "Load More" button with smooth animations
 * - **PageControls**: Traditional pagination with page numbers and navigation
 *
 * Both components feature:
 * - TrueTone AI brand styling (orchid/indigo gradients)
 * - Loading states and error handling
 * - Full accessibility (ARIA labels, keyboard navigation)
 * - Smooth animations using Motion
 * - Mobile-first responsive design
 */

export { InfiniteScroll } from './InfiniteScroll';
export { PageControls } from './PageControls';
