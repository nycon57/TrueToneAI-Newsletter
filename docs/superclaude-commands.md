```
/sc:analyze is running… @src/lib --focus performance --think-hard "Output your analysis in a clean, well-formatted markdown document in @docs/ with a clear name and timestamp"
```

```
/sc:analyze is running… --focus quality --depth deep  ./src/components/analytics/copy-button.tsx ./src/components/newsletter/like-button.tsx "Review, analyze, evaluate, and identify any linting or build errors. Then fix optimize the code, so that it 
correctly passes all linting and build checks"
```